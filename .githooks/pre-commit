#!/bin/sh
set -e

# Sync package.json version on release/hotfix branches.
node scripts/version-sync.mjs

# If the script changed package.json, include it in the commit.
git add package.json 2>/dev/null || true

